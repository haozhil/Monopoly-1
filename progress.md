Original prompt: 我现在希望扩充一下左边的棋盘,要从现在的4*4扩充到8*8,并且先试着做一个联通的桥梁,就比如说从老街到税务局可以选择两条路,你做一系列的格子作为第二条路,这个第二条路之后会有独特的设计,现在你可以先用普通的格子来代替,当玩家走到岔路时需要在地图上选择走那条路的提示来让玩家选择

- 已把棋盘常量扩到 8x8 外圈，主路线从 12 格扩到 28 格，并在老街与税务局之间新增 5 格桥梁支路。
- 移动逻辑已从简单的 `position + 1` 改成按 `nextIds / previousId` 走图，支持岔路口路线选择。
- 人类玩家走到老街后的岔路会暂停移动并等待地图点击；AI 会自动选主路或桥路。
- 棋盘增加了路线选择浮层和目标格按钮，侧栏会提示去左侧棋盘选路。
- 已补 `window.render_game_to_text` 方便 Playwright 读取当前状态。
- 已修复 `Avatar` 的 React SVG 属性警告，避免测试日志被无关错误污染。
- 已验证：开局后把骰子固定到 2 点，玩家 1 会在老街触发路线选择；点击 `前往 旧桥口` 后，角色会落到桥路第一格并进入购买阶段。
- 已把回合推进、落点结算、机会卡定义与结算结果抽到 `src/game/engine/`，`App.js` 现在主要保留动画、等待与 UI 事件编排。
- 抽离后的 engine 回归已通过：`route-choice` 链路在新结构下仍可正常进入桥路并触发购买。
- 已新增开局初始资金档位选择，当前支持 `¥1000 / ¥1500 / ¥2000 / ¥3000`。
- 已新增持有者着色：购买地产后，地块会按所选卡通人物的主题色显示背景和边框。
- 岔路规则已调整为“停在岔路口后再选择下一回合路线”，不再在移动经过时中断当前步数。
- 已回归验证：掷出 1 点停在老街时可正常购买；购买后才会弹出路线选择。掷出 3 点经过老街时不会弹路线选择，会继续落到港口。
- 已修复“几回合后卡住”的状态机漏口：普通落点（如奖金池）结算后现在会正常推进到下个回合，不会停在 `hasRolledThisTurn=true` 的死状态。

TODO
- 用 Playwright 再补一条主路线选择（前往机会）和更大点数穿越桥路的回归用例。
- 继续把 `runTurn`、投骰与更细的状态机也迁到 engine，逐步把 `App.js` 缩成 UI shell。
- 后续如果要把桥路做成独特区域，可以继续给 28-32 号格子换类型或引入独立规则集。
